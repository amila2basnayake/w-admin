# Waterfind

Working repository for the Waterfind engagement (broker/staff tooling for Australia's water-trading
exchange).

**Greenfield — no stack or architecture chosen yet.** See [`CLAUDE.md`](./CLAUDE.md) for working
conventions and the current status. Don't scaffold an app or add dependencies until the approach is
decided and recorded.

## Layout

- `CLAUDE.md` — guide for working in this repo (read first).
- `.claude/` — Claude Code tooling: `settings.json` (permissions), `agents/`, `skills/`.
- `docs/design/` — design memos, written before non-trivial work.
- `.env.example` — copy to `.env` for local config (gitignored).
